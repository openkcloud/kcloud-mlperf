apiVersion: v1
kind: Namespace
metadata:
  name: npu
  labels:
    name: npu
    app.kubernetes.io/part-of: etri-llm
    # Shared with Lane B / WS-N* manifests — both lanes target this namespace
    # for vendor-blessed in-cluster NPU inference servers.
    accelerator.etri/scope: in-cluster-inference
